.title
  .row
    .columns
      %h1
        %i.fa.fa-cogs.fa-fw
        Settings
%table.responsive.full
  %thead
    %tr
      %td Name
      %td Value
      %td
        = link_to new_admin_setting_url do
          %i.fa.fa-plus.fa-fw
          Add
  %tbody
    - settings.each do |s|
      %tr
        %td
          = link_to edit_admin_setting_url(s) do
            %i.fa.fa-pencil.fa-fw
            = s.name
        %td
          - if s.value
            = s.value
          - else
            %i.fa.fa-circle.fa-fw
        %td
          - if s.system
            %i.fa.fa-lock.fa-fw
          - else
            %i.fa.fa-unlock.secondary.fa-fw
          = s.slug_id